import { YthShopValidators } from './yth-shop-validators';

describe('YthShopValidators', () => {
  it('should create an instance', () => {
    expect(new YthShopValidators()).toBeTruthy();
  });
});
